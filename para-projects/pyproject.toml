[project]
name = "para-projects"
version = "0.1.0"
description = "PARA project manager skill for Claude Code"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
